本工程是自己写的Cmakelist工程,主要是想结合ceres库做一个C++版本的手眼标定程序,框架基本已经搭好,但是最终结果不对,就是单步调试进不去ceres的优化函数,我在hand_eye_calibrator.h的ReprojectionError的operator中写了一个cout<<"aaa",但是程序并没有输出,所以觉得优化函数并没有执行,但是由于时间的关系,没再去找bug,就放到这里了,整个程序是没有编译错误的,框架应该也是对的,就是调用不了优化函数,以后交给师弟调试吧
